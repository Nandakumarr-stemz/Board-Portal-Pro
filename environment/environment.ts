// Single variable to switch environment manually
export const ACTIVE_ENV = "PROD"; // Change to "LOCAL", "UAT", or "PROD"

export const environment = {
  production: true,
  apiUrl: "http://localhost:3000/api",
  BASE_PATH: "/Board-Portal-Pro/"
};

// 